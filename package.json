{
  "name": "brand-research",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve . --listen 3000 --no-clipboard",
    "start": "npx serve . --listen 3000 --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
